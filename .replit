run = "bash -lc 'cd backend && pip install -r requirements.txt && uvicorn app.main:app --host 0.0.0.0 --port 8000 & cd frontend && npm install && npm run dev -- --host 0.0.0.0 --port 5173'"

[nix]
channel = "stable-24_05"
